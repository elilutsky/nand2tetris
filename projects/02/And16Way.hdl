
/**
 * 16-way And: 
 * out = (in[0] and in[1] and ... and in[15])
 */

CHIP And16Way {
    IN in[16];
    OUT out;

    // And(a_0, ..., a_15) = Not (Not(a_0) or ... or Not(a_15)) 
    PARTS:
    
    Not16(in=in, out=notIn);
    Or16Way(in=notIn, out=orNotIn);
    Not(in=orNotIn, out=out);
} 
